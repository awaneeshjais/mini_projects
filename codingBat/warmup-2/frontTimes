public String frontTimes(String str, int n) {
  int leng = str.length();
  String s = "";
  
  if (leng < 3 ) {
  for (int i = 0; i<n; i++){
  s = s + str;
  }
  }else {
  for (int i = 0; i<n; i++){
  s = s + str.substring(0,3);
  }
  }
  return s;
}