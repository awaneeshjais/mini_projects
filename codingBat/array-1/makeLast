public int[] makeLast(int[] nums) {
  int l = nums.length;
  int l2 = l*2;
  int[] arr = new int[l2];
  for (int i = 0;i < arr.length;i++){
  arr[i] = 0;
  }
  arr[l2-1] = nums[l-1];
  
  return arr;
  
}
