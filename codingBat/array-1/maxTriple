public int maxTriple(int[] nums) {
int max = 0;
int [] tmp = {nums[0],nums[(nums.length-1)/2],nums[nums.length-1]};
for (int i=0; i<tmp.length;i++){
if (max<tmp[i]){
max=tmp[i];
}else{
max=max;
}
}
return max;
}